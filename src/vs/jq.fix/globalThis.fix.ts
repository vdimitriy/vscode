/* eslint-disable local/code-import-patterns */
/* eslint-disable header/header */

export const _globalThis = typeof globalThis === 'object' ? globalThis : (typeof self === 'object' ? self : window);
